# Dior Backoffice Guide

This note is intentionally generic for public sharing.

## Development

- Start the app with `npm start`
- Configure the API base URL through `.env.local`
- Use the public README for the high-level project overview
- Keep environment-specific URLs, credentials, and tokens out of the repository
