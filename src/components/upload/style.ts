import styled from 'styled-components'

export const Container = styled.div`
	width: 50vw;
	max-width: 550px;
	position: relative;
	padding: 25px 45px 45px;
	.exit {
		top: 15px;
		left: 15px;
		position: absolute;
	}
	h3 {
		font-size: 24px;
		font-weight: 700;
		text-align: center;
		color: var(--gray80);
		margin-bottom: 50px;
	}
	.divider {
		margin: 30px 0;
	}
`
