import styled from 'styled-components'

export const Container = styled.div`
	display: flex;
	align-items: center;
	flex-direction: column;
	h6 {
		font-size: 20px;
		font-weight: 400;
		margin-bottom: 3px;
		text-align: center;
		color: var(--gray80);
	}
`
