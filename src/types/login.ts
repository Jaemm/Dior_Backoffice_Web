export type CredentialsFormTypes = {
	email: string
	password: string
	remember?: boolean
}

export type OktaFormTypes = {
	email: string
	remember?: boolean
}

export type FormForgetTypes = {
	email: string
}
